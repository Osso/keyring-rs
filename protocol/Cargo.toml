[package]
name = "keyring-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Protocol types for keyring-rs unlock IPC"

[dependencies]
serde = { version = "1", features = ["derive"] }
rmp-serde = "1"

[dev-dependencies]
